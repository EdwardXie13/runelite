name: Sync Fork

on: [ push, pull_request ]
  schedule:
    - cron: '*/30 * * * *' # every 30 minutes

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.4
        with:
          base: master
          head: master